[build]
  base = "client"
  command = "yarn build"
  publish = "dist"
  functions = "backend"

[build.environment]
  NODE_VERSION = "16"

[[redirects]]
  from = "/api/*"
  to = "http://anmeldung.infinityfreeapp.com/:splat"
  status = 200
